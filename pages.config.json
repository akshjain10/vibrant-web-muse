
{
  "buildCommand": "npm install --no-package-lock && npm run build && node cloudflare-worker.js",
  "outputDirectory": "dist",
  "installCommand": "npm install --no-package-lock",
  "framework": "vite",
  "buildCache": true
}
